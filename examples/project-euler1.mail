; https://projecteuler.net/problem=1

(begin
  (define sum 0)
  (define i 1)
  (while (< i 1000)
    (if (or (= (% i 3) 0) (= (% i 5) 0))
        (set! sum (+ sum i))
        unit)
    (set! i (+ i 1)))
  sum)
